startDelay = 0.3;
pause = if(renderMode == RENDERMODES.FRAMES, 1, 0.1);

trackData = [
    1, pause,   // 01   
    1, pause,   // 02   
    1, pause,   // 03   
    1, pause,   // 04   
    1, pause,   // 05   
    1, pause,   // 06   
    1, pause,   // 07   
    1, pause,   // 08   
    1, pause,   // 09   
    1, pause    // 10   
];

delayedSetup() := (

);

calculation() := (

);

rendering() := (

);