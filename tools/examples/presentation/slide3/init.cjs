renderMode = RENDERMODES.STEPS;
showDebugInfo = false;



startDelay = 0.5;

trackData = [
    1, 1,
    1, 1,
    1, 1,
    1, 1
];

objectSetup() := (

);

calculation() := (

);

rendering() := (

);

